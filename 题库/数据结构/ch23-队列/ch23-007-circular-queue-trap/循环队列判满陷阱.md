# 循环队列判满陷阱

## 考点

循环队列的经典陷阱：`front == rear` 时无法直接区分"空"还是"满"。

## 思路

额外维护一个 `cnt` 变量记录当前元素个数，所有的"满/空"判断都靠 `cnt` 而不是靠下标猜——入队前查 `cnt==m`，出队前查 `cnt==0`，成功操作后同步更新 `cnt`。这是最简单可靠、不容易出错的做法。

## 复杂度

每个操作 `O(1)`，总复杂度 `O(n)`。
