## 题目描述

上一题的循环队列保证了操作合法，这一题反过来——请你实现一个容量为 `m` 的循环队列，并且要**正确处理队满、队空的情况**：

- `1 x`：入队 `x`。如果队列已经**满**了（已有 `m` 个元素），输出 `Full`，并且**不要**真的把 `x` 放进去。
- `2`：出队。如果队列是**空**的，输出 `Empty`，不做其他操作；否则正常出队，输出被移除的元素。
- `3`：查询当前队列中的元素个数。
