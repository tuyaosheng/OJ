# 滑动窗口最大值

## 考点

单调队列：把暴力 `O(nk)` 优化到 `O(n)` 的经典模型，本章的核心考点。

## 思路

维护一个存下标、值从队首到队尾严格递减的双端队列。每来一个新元素，先从队尾弹出所有 `≤` 它的下标（它们不可能再当选最大值），把新下标压入队尾；再检查队首是否已滑出窗口，滑出就弹出。窗口填满后，队首对应的值就是当前窗口最大值。

## 复杂度

每个下标最多入队出队各一次，`O(n)`。
