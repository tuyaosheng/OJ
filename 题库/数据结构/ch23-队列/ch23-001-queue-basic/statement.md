## 题目描述

队列（Queue）是一种只能在一端进、另一端出的线性表结构，特点是**先进先出**（FIFO, First In First Out）——最早进队的元素，最早出队。就像排队买票，先来的人先办理。

请你模拟一个队列，依次处理 `n` 个操作：

- `1 x`：元素 `x` 入队（从队尾进）。
- `2`：出队（从队首出），输出被移除的元素。
- `3`：查询当前队列的大小。

数据保证执行操作 `2` 时队列不为空。
