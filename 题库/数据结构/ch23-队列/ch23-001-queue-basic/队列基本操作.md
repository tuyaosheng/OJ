# 队列基本操作

## 考点

STL `queue` 的基本用法：`push`、`pop`、`front`、`size`。

## 思路

严格按题意模拟：`1 x` 就 `q.push(x)`；`2` 先 `q.front()` 记下要输出的值再 `q.pop()`（`pop()` 本身不返回值）；`3` 直接输出 `q.size()`。

## 复杂度

每个操作 `O(1)`，总复杂度 `O(n)`。
