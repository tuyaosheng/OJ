- C++ 标准库自带 `queue<int>`，直接 `push`（入队，加在队尾）、`pop`（出队，去掉队首，注意它不返回值）、`front`（查看队首）、`size`（当前元素个数）即可。
- 操作 `2` 要先用 `front()` 记下要输出的值，再 `pop()`。
- 和栈（`stack`）对照着学：接口几乎一样，只是 `stack` 的 `top()` 看的是最后进去的那个，`queue` 的 `front()` 看的是最早进去的那个。
