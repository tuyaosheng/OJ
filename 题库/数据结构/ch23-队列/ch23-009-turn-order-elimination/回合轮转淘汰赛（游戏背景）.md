# 回合轮转淘汰赛（游戏背景）

## 考点

队列的"轮转/淘汰"模型（约瑟夫问题的简化变体）+ 遍历输出的实现细节。

## 思路

`1..n` 入队；每个事件先弹出队首，结果为存活就压回队尾，阵亡就丢弃。全部处理完后遍历剩余队列输出——因为 `queue` 不能直接遍历，改用 `deque` 实现（接口和 `queue` 一样，还能 `for` 遍历）。

## 复杂度

`O(n + m)`。
