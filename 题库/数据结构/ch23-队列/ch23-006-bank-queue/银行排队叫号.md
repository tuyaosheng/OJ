# 银行排队叫号

## 考点

多队列调度：现实中常常不止一个队列，需要按规则决定新元素进哪一条。

## 思路

用 `m` 个 `queue<int>` 分别代表每个窗口。顾客到达时线性扫描 `m` 个窗口找排队人数最少（并列取编号小）的那个，把顾客编号压入其队尾；叫号就是对指定窗口做一次出队。

## 复杂度

每次到达 `O(m)`，每次叫号 `O(1)`，总复杂度 `O(nm)`，在 `m ≤ 100` 时足够快。
