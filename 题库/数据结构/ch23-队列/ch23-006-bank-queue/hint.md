- 用 `m` 个队列（比如 `queue<int> q[105]`）分别表示每个窗口的排队情况。
- 顾客到达时，遍历 `m` 个窗口，找出 `q[i].size()` 最小的窗口（并列时选下标最小的那个），把顾客编号 `push` 进那个窗口的队列。因为 `m ≤ 100`，每次遍历只有 `O(m)`，足够快。
- 记得维护一个"到达顾客计数器"，每来一位新顾客就加一，这个计数器的值就是他的编号。
- 窗口叫号就是对指定窗口的队列做一次 `front()` + `pop()`。
