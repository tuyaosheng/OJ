## 题目描述

某银行有 `m` 个窗口，编号 `1` 到 `m`，每个窗口各排一条队。请你依次处理 `n` 个事件：

- `1`：一位新顾客到达。他的编号就是"这是第几位到达的顾客"（第一位到达的顾客编号为 `1`，第二位为 `2`，以此类推）。这位顾客会选择**当前排队人数最少**的窗口排队；如果有多个窗口排队人数并列最少，选**编号最小**的那个。
- `2 w`：`w` 号窗口叫号，该窗口排在最前面的顾客办理完毕离开，请你输出这位顾客的编号。

数据保证执行操作 `2 w` 时，`w` 号窗口的队伍不为空。
