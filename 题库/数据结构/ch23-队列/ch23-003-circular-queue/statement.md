## 题目描述

请你**不使用** STL 的 `queue`，改用一个**固定大小为 `m` 的数组**手写一个循环队列，支持：

- `1 x`：元素 `x` 入队。
- `2`：出队，输出被移除的元素。

数据保证操作过程中队列中的元素个数**始终不超过 `m`**，且执行操作 `2` 时队列不为空——也就是说你不需要处理"队满"或"队空"时的异常情况，只需要正确实现循环队列的入队出队逻辑。
