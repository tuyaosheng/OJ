# 循环队列模拟

## 考点

用固定大小数组手写循环队列，核心是下标取模。

## 思路

`front`/`rear` 两个下标在 `[0, m)` 范围内循环移动：入队 `a[rear]=x; rear=(rear+1)%m;`，出队 `输出 a[front]; front=(front+1)%m;`。取模让数组"首尾相连"，避免了普通数组模拟队列时前面空间被浪费的问题。

## 复杂度

每个操作 `O(1)`，总复杂度 `O(n)`。
