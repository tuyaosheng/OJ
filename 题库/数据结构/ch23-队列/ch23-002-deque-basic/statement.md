## 题目描述

**双端队列**（deque, double-ended queue）是队列的加强版——两端都能进、都能出。请你模拟一个双端队列，依次处理 `n` 个操作：

- `1 x`：元素 `x` 从**队尾**入队。
- `2 x`：元素 `x` 从**队首**入队。
- `3`：从**队尾**出队，输出被移除的元素。
- `4`：从**队首**出队，输出被移除的元素。

数据保证执行操作 `3` 和 `4` 时双端队列不为空。
