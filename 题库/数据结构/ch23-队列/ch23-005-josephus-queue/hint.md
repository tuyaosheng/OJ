- 把 `1` 到 `n` 依次入队，模拟"围成一圈"。
- 每一轮报数，等价于：把队首"轮转" `k - 1` 次（每次弹出队首、立刻把它压回队尾——相当于这个人报了数但没出列，回到圈子里排到最后），第 `k` 次直接把队首**弹出并记录**（不再放回，这个人出列了）。
- 重复上面这个过程，直到队列为空，记录下来的顺序就是答案。
- 为什么"轮转 k-1 次再弹出"是对的：队列的相对顺序始终保持"围成一圈"的样子，每转一次相当于报数往前走一个人，走了 `k-1` 步之后队首正好是第 `k` 个人。
