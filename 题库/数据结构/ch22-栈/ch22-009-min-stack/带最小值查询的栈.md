# 带最小值查询的栈

## 考点

辅助栈技巧：用一个和主栈同步进出的辅助栈，把"查询最小值"从 O(n) 优化到 O(1)。

## 思路

辅助栈 `minSt` 的栈顶始终是"主栈从栈底到当前"的最小值。push 时，如果新值不大于 `minSt` 当前栈顶就同步压入新值，否则把 `minSt` 的栈顶再复制一份压入（保持两栈高度一致）；pop 时两个栈一起弹。查询直接输出 `minSt.top()`。

## 复杂度

每个操作 `O(1)`，总复杂度 `O(n)`。
