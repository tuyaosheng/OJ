# 仓库栈式存取

## 考点

STL `stack` 的基本用法：`push`、`pop`、`top`、`size`。

## 思路

严格按题意模拟即可：

- `1 x`：`st.push(x)`。
- `2`：先 `st.top()` 记下要输出的值，再 `st.pop()`。`pop()` 本身不返回值，必须分两步。
- `3`：直接输出 `st.size()`。

数据保证操作 `2` 时栈非空，不需要额外判空。

## 复杂度

每个操作都是 `O(1)`，总复杂度 `O(n)`。
