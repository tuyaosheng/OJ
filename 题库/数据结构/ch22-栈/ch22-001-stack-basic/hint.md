- C++ 标准库自带 `stack<int>`，直接 `push`（入栈）、`pop`（出栈，注意它不返回值）、`top`（查看栈顶）、`size`（当前元素个数）即可，不需要自己写数组模拟。
- 操作 `2` 要先用 `top()` 记下栈顶的值再 `pop()`，因为 `pop()` 本身没有返回值。
- 也可以用一个数组 + 一个"栈顶指针" `top` 手写栈：入栈是 `a[++top] = x`，出栈是 `a[top--]`，这样能更直观地看到栈底层是怎么实现的。
