## 题目描述

仓库用一个货架来暂存包裹：新到的包裹只能放在最上面，取件时也只能从最上面拿——这就是**栈**（后进先出，LIFO）。

请你模拟这个货架，依次处理 `n` 个操作：

- `1 x`：一个编号为 `x` 的包裹入库（压入栈顶）。
- `2`：取出最上面的包裹（弹出栈顶），并输出它的编号。
- `3`：查询当前货架上还剩多少个包裹（栈的大小）。

数据保证执行操作 `2` 时货架不为空。
