# 出栈序列合法性判定

## 考点

栈的性质与贪心模拟：判断给定序列是不是某个"1..n 依次入栈、任意时刻出栈"过程的合法出栈顺序。

## 思路

维护"下一个待入栈的数" `nxt`（从 1 开始）和一个真实的栈。扫描目标序列的每个数 `x`：栈顶等于 `x` 就直接弹出；否则不断把 `nxt` 压栈（`nxt++`），直到栈顶变成 `x` 或者 `nxt` 超过 `n`（超过就说明不合法）。

贪心正确性：能弹就弹是最优的——不弹的话 `x` 只会被埋得更深，没有任何好处。

## 复杂度

`nxt` 从 1 到 n 只走一遍，每个数入栈出栈各一次，`O(n)`。
