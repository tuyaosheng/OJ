- 维护一个存**下标**的栈，栈里对应的元素值**从栈底到栈顶严格递减**（单调递减栈）。
- 从左到右扫描 `a`：对当前元素 `a[i]`，只要栈非空且**栈顶下标对应的值 < a[i]**，说明 `a[i]` 就是那个下标的"右边第一个更大元素"——弹出栈顶并记录答案，重复这个过程直到栈顶的值 ≥ a[i]（或栈空）。
- 处理完弹栈后，把下标 `i` 压入栈（它可能是后面某个更小元素的答案）。
- 最后栈里剩下的下标，说明右边没有比它们更大的元素，答案是 `-1`。
- 为什么是 `O(n)`：每个下标最多入栈一次、出栈一次，总操作次数不超过 `2n`。
