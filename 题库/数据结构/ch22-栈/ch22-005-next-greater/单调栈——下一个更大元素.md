# 单调栈——下一个更大元素

## 考点

单调栈：把暴力 `O(n^2)` 优化到 `O(n)` 的经典模型。

## 思路

维护一个存下标、值从栈底到栈顶严格递减的栈。扫描到 `a[i]` 时，不断弹出"值 < a[i]"的栈顶下标，把它们的答案记为 `a[i]`（因为 `a[i]` 就是它们右边第一个更大的元素），然后把 `i` 压栈。扫描结束后栈里剩下的下标答案都是 `-1`。

## 为什么是 O(n)

每个下标最多入栈一次、出栈一次，总操作次数不超过 `2n`，均摊下来每个元素 `O(1)`。

## 复杂度

`O(n)`。
