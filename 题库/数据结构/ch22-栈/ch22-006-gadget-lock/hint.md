- 这题和例 2「括号匹配」的算法**完全一样**，只是把 3 种括号换成了 26 种字母大小写配对——遇到大写字母（触发）就压栈；遇到小写字母（复位）时，检查栈顶：如果栈空，或者栈顶的大写字母**不是**它对应的触发字母，就不合法；否则弹出栈顶继续扫描。
- 判断"栈顶是不是对应的触发字母"：把栈顶字符转成大写（或者复位字符转成小写）比较即可，比如 `toupper(c)` 或者直接 `c - 32`（小写字母的 ASCII 码比对应大写字母大 32）。
- 扫描完字符串后，栈必须**恰好为空**，才说明所有触发都被复位了。
