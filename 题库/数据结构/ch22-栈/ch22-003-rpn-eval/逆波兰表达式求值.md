# 逆波兰表达式求值

## 考点

栈在表达式求值中的应用；后缀表达式不需要括号和优先级的原理。

## 思路

从左到右扫描 token：数字入栈；运算符则弹出栈顶两个数（先弹出的是右操作数 `b`，后弹出的是左操作数 `a`），计算 `a op b` 后把结果压回栈。扫完后栈里唯一剩下的数就是答案。

判断 token 是数字还是运算符：运算符 token 长度恒为 1，负数 token 长度至少为 2，用长度就能区分。

## 复杂度

每个 token 处理一次，`O(n)`（`n` 为 token 个数）。
