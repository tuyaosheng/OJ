## 题目描述

**逆波兰表达式**（又叫**后缀表达式**）把运算符写在两个操作数**之后**，比如中缀表达式 `(3 + 4) * 2` 写成后缀就是 `3 4 + 2 *`。它的好处是不需要括号、不需要考虑运算符优先级，从左到右扫描配合一个栈就能直接算出结果。

给你一个后缀表达式，以空格分隔各个 token（数字或运算符 `+ - * /`），求出它的值。

除法为**整数除法**，数据保证每次做除法时都能整除（不会出现除不尽的情况），也保证除数不为 0。
