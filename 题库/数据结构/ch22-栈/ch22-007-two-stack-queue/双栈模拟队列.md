# 双栈模拟队列

## 考点

用两个栈拼出一个先进先出的队列——栈和队列之间的经典结构变形。

## 思路

`in` 栈接收所有新入队的元素；`out` 栈只在自己为空时，才把 `in` 栈整体倒进来（这一倒，顺序就反过来了，变成先进的在栈顶）。出队时优先从 `out` 弹，`out` 空了再从 `in` 倒一批过来。

## 复杂度

每个元素最多被"倒"一次，均摊 `O(1)` 每次操作，总复杂度 `O(n)`。
