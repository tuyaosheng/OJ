## 考点

二叉树的结构变换——递归交换每个节点的左右子树。

## 思路

"翻转整棵树"可以拆成"翻转左子树" + "翻转右子树" + "交换根节点的左右孩子"三部分，天然是递归结构。递归到空节点直接返回（空树的镜像还是空树）。交换操作只涉及指针本身，不需要拷贝节点内容，效率是 $O(n)$。

## 易错点

- 只交换根节点的左右孩子，忘记递归处理子树，会导致只有第一层被翻转。
- `swap(root->left, root->right)` 交换的是指针变量，不是节点内容，理解这一点才能明白为什么递归顺序（先交换再递归，或先递归再交换）都能得到正确结果。
