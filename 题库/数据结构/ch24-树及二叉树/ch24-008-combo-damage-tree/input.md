一行，用空格分隔的 token，表示一个合法的前缀表达式。每个 token 要么是运算符 `+` 或 `*`，要么是一个非负整数（$0 \le$ 数值 $\le 10^6$）。

token 总数不超过 $1001$（即表达式树节点数不超过 $1001$，运算符恰好比数字少 $1$ 个）。保证表达式合法，且整个表达式的最终结算值不超过 $10^{18}$（用 `long long` 存储足够）。
