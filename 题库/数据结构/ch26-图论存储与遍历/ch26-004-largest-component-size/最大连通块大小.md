## 考点

连通块问题的另一个常见变体：不只是数个数，还要统计每个连通块的规模。

## 思路

在遍历过程中顺便计数是最自然的写法——每处理（出队）一个点就把计数器加一，遍历结束后这个计数器就是这一整个连通块的大小。外层循环仍然是"对每个未访问的点触发一次遍历"，只是这次要在多个连通块的大小里取最大值。

## 易错点

- 计数要在 `bfs` 内部完成并返回，不能在外层单独再数一遍（那样等于重新遍历，多此一举）。
- 孤立点的连通块大小是 1，也要参与比较，容易被误认为"不算一个连通块"而漏掉。
- 图连通（只有一个连通块）时，答案就是 $n$，这是一个很好的自查用例。
