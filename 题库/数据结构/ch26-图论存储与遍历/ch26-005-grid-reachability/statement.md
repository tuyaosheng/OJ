很多"迷宫"类问题表面上看不出是图论题，但其实可以直接转化成图：把**每个格子当成一个节点**，**相邻（上、下、左、右四个方向）且都不是墙的两个格子之间连一条边**，转化完成后，"能否从起点走到终点"就变成了标准的图上可达性问题，直接套用 DFS 或 BFS 即可。

给定一个 $R$ 行 $C$ 列的网格，每个格子是空地（`.`）或墙（`#`），只能在空地之间沿上下左右四个方向移动，不能斜着走，也不能走出网格或穿过墙。给定起点和终点（保证都是空地），判断能否从起点走到终点。
