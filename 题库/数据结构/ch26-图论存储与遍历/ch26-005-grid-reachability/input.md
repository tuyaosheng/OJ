第一行两个整数 $R, C$（$1 \le R, C \le 1000$）。

接下来 $R$ 行，每行一个长度为 $C$ 的字符串，只包含 `.`（空地）和 `#`（墙）。

最后一行四个整数 $sr, sc, tr, tc$（$1 \le sr, tr \le R$，$1 \le sc, tc \le C$），表示起点坐标 $(sr, sc)$ 和终点坐标 $(tr, tc)$（行、列均从 $1$ 开始编号）。保证起点和终点都是空地。
