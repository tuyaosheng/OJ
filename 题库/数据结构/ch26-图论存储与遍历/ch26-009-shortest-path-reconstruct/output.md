如果 $t$ 不可达，输出一行 `-1`。

否则输出两行：第一行是最短路径的长度（边数）；第二行是路径上依次经过的节点编号，用空格分隔，从 $s$ 到 $t$。

最短路径可能不唯一，本题按照本章统一约定的规则确定唯一输出：邻接表按编号从小到大排序，BFS 时按顺序访问未访问的邻居（和例1、例2完全一致的规则）——这样"每个点第一次被访问时是从哪个点走过来的"就是确定的，从而得到一条唯一确定的最短路径，请严格按此规则输出。
