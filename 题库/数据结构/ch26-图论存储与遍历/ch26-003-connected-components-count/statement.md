一张无向图不一定是"浑然一体"的——它可能由若干个互不相连的部分组成，每一部分内部任意两点都能互相到达，不同部分之间则完全没有边相连。这样的每一部分叫做一个**连通块**（连通分量）。

给定一张无向图（$n$ 个点，可能有孤立点，也可能不连通），求这张图一共有多少个连通块。

**做法**：准备一个 `visited` 数组，从编号 $1$ 到 $n$ 依次检查——如果某个点还没被访问过，说明发现了一个"新的"连通块，把连通块数量加一，并从这个点出发做一次完整的 DFS 或 BFS，把这一整块里的点全部标记为已访问；继续检查下一个编号，直到所有点都被访问过。
