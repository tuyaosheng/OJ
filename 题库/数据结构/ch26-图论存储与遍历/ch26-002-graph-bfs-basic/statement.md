和树的层序遍历一样，图也有对应的"一层一层往外扩展"的遍历方式——**广度优先遍历（BFS）**。

从给定起点 $s$ 出发，用一个队列维护"待访问"的点：先把 $s$ 入队；每次从队首取出一个点，把它**所有未访问过的邻居**（按编号从小到大的顺序）依次标记为已访问并入队；重复直到队列为空。

请你输出从 $s$ 出发的 BFS 访问顺序。

> BFS 有一个非常重要的性质：如果所有边的权值都相同（或者说不带权），BFS 第一次到达某个点时，走过的边数就是从 $s$ 到这个点的**最短距离**。这个性质会在本章练习题和下一章"最短路"里反复用到。
