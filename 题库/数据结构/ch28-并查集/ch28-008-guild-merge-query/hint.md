和例2、练2完全一样的"按大小合并"模板，查询时直接输出 `sz[find(u)]`（$u$ 所在树根的 `size` 就是这个战队当前的总人数）：

```cpp
if (op == 1) {
    merge(u, v);
} else {
    cout << sz[find(u)] << '\n';
}
```

注意 `sz` 数组只在**根节点**上是准确的，非根节点的 `sz` 值可能是合并前的旧数据——所以查询时一定要先 `find(u)` 拿到根，再去取 `sz`，不能直接写 `sz[u]`。
