第一行三个整数 $n, m, s$（$1 \le n \le 2000$，$0 \le m \le 10000$，$1 \le s \le n$）。

接下来 $m$ 行，每行三个整数 $u, v, w$（$1 \le u, v \le n$，$u \ne v$，$1 \le w \le 1000$），表示一条从 $u$ 到 $v$、权值为 $w$ 的有向边。图中可能存在重边（取哪一条效果都一样，Dijkstra 的松弛过程会自动选到更优的）。
