第一行三个整数 $n, m, s$（$1 \le n \le 100000$，$0 \le m \le 200000$，$1 \le s \le n$）。

接下来 $m$ 行，每行两个整数 $u, v$（$1 \le u, v \le n$，$u \ne v$），表示一条无向边。图中可能存在重边，**每条重边都算作一条不同的走法**（比如 $u,v$ 之间有两条重边，那么"从 $u$ 走一步到 $v$"就算两种不同的走法）。
