直接复用例2的堆优化 Dijkstra 代码：以 $s$ 为起点跑一遍，输出 `dist[t]`（如果是 `INF` 就输出 `-1`）即可，不需要任何额外处理——这道题就是想让你确认自己已经能熟练、独立地写出这套模板，为后面综合应用打好基础。

$n, m$ 的数据范围和例2一致，务必使用堆优化版本（$O((n+m)\log n)$），朴素 $O(n^2)$ 版本在 $n=10^5$ 时会超时。
