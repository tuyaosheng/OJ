样例 1：a=1，k=10。累计：1 秒后 1，2 秒后 3，3 秒后 7，4 秒后 15 ≥ 10，答案 4。

"**攒到什么时候才够**"——次数未知、条件已知，标准的 while 场景：

```cpp
while (sum < k) { sum += gain; gain *= 2; t++; }
```

**注意 k 高达 10^17**，早就超出 int（约 2.1×10^9）的范围——`sum`、`gain`、`k` 统统要用 **long long**（读入用 `cin` 或 `%lld`）。这是本题真正想考你的事。
