逐格扫描：遇到 `*` 原样输出；遇到 `.` 就数它周围 8 格里有几个 `*`。

"周围 8 格"不要写 8 个 if——用**双重小循环**枚举行偏移 dx 和列偏移 dy（各取 -1,0,1，跳过 0,0），或者背这对方向数组：

```cpp
int dx[] = {-1,-1,-1, 0, 0, 1, 1, 1};
int dy[] = {-1, 0, 1,-1, 1,-1, 0, 1};
```

**边界格子**的邻居会越界（比如左上角只有 3 个邻居），访问前必须判断 `0 <= nx && nx < n && 0 <= ny && ny < m`。
