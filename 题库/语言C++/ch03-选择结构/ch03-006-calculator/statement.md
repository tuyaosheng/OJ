一个最简单的计算器，支持 +、-、*、/ 四种运算。仅需考虑输入输出为整数的情况，数据和运算结果不会超过 int 的表示范围。

要求考虑以下两种特殊情况：
1. 如果出现除数为 0 的情况，输出：`Divided by zero!`
2. 如果出现无效的操作符（即不是 +、-、*、/ 之一），输出：`Invalid operator!`

除法按整数除法处理（舍去小数部分）。

（本题对应课件例 3.10。）
