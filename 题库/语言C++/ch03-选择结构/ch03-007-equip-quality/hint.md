用 **if - else if 链**从小到大依次判断。每个 `else if` 自带"前面都不满足"的隐含条件，所以只需要写上界：

```cpp
if (v < 100) ...
else if (v < 300) ...   // 走到这里时必然 v >= 100，不用再写
```

**边界值**是本题的考察重点：99 是白色，100 就是绿色；999 是紫色，1000 就是橙色——用样例和边界自己核对一遍。
