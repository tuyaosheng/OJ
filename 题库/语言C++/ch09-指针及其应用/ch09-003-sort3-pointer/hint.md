普通函数的参数是"值传递"，函数里改了不影响外面。要让函数**真正改动**主函数的变量，就把变量的**地址**传进去：`void sort3(int *x, int *y, int *z)`，函数里用 `*x`、`*y`、`*z` 直接操作原变量。
交换用指针版：`int t = *x; *x = *y; *y = t;`。这就是指针最重要的用途之一——让函数能修改外部数据（`swap` 的底层原理）。
