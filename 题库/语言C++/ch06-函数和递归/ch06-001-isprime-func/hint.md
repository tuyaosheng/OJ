把第 4 章的试除法逻辑**封装成函数**：`bool isPrime(int n)`。
函数好处：主函数只写一句 `if (isPrime(n)) ...`，逻辑清晰、可复用。
边界要在函数里处理：n < 2 直接返回 false；试除到 `i*i <= n`。注意 n 可到 20 亿，参数用 int 够（上限 21 亿），但 `i*i` 会溢出——把 i 声明为 long long 或写成 `i <= n/i`。
