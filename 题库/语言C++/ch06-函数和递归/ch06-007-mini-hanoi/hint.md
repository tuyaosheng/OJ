把递推式原封不动翻译成递归函数：

```cpp
long long T(int n) {
    if (n == 1) return 1;        // 边界
    return 2 * T(n - 1) + 1;     // 递归式
}
```

递归两要素齐了：**边界**（n=1 返回 1）、**规模变小**（n → n−1）。

n = 60 时答案是 2^60 − 1 ≈ 1.15 × 10^18，超出 int 十亿量级的上限——返回值必须 **long long**。
