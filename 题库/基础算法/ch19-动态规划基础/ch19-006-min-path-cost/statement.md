## 题目描述

这是【例2】过河卒的直接变式：把"求方案数"换成"求最优值"，同时保留"有些格子不能走"这个设定。

一个 `n` 行 `m` 列的网格，每个格子要么是**可以通过的**（标注一个正整数，表示经过这个格子要花费的代价），要么是**障碍物**（完全不能经过）。你从左上角 `(1, 1)` 出发，每一步只能**向下**或**向右**移动一格，目标是到达右下角 `(n, m)`。请你求出走到终点的最小总代价（路径上每个格子的代价都要计入，包括起点和终点）；如果无法到达终点，输出 `-1`。
