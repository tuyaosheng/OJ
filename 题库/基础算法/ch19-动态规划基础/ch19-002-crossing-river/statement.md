## 题目描述

【例1】数字三角形是"求最优值"的 DP，这题练"**求方案数**"的 DP——DP 三要素完全一样，只是转移时把"取更优的一个"换成"把两种方式的方案数加起来"。

棋盘上一个卒，从坐标 `(0, 0)` 出发，每一步只能**向下**或**向右**移动一格，目标是走到坐标 `(n, m)`。棋盘上另有一个棋子"马"，位于坐标 `(bx, by)`。卒不能经过马所在的格子，也不能经过马**一步能跳到**的格子（也就是中国象棋里"马走日"能到达的所有格子）。请你求出卒从 `(0, 0)` 走到 `(n, m)`，一共有多少种不同的走法。
