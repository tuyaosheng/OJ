上一题里 b 还小到能塞进 `int`。现在把限制彻底放开：**两个大整数相乘**。

给定两个非负整数 a 和 b（都可能非常大），请计算 a × b。

再想想小学的竖式乘法：用 b 的每一位去乘 a 的每一位，然后把这些乘积**错位相加**。所谓"错位"，其实就是——

> a 的第 i 位（从 0 数起）乘 b 的第 j 位，得到的结果应该加到答案的第 **i + j** 位上。

这条 `i + j` 的规律是整个高精度乘法的灵魂，想通了它，代码就是一个二重循环。
