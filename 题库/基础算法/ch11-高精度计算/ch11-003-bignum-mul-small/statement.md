加法和减法都会了，现在轮到乘法。我们先从简单的情形入手：**一个大整数乘以一个小整数**（小到 `int` 装得下）。

给定一个非常大的非负整数 a，和一个较小的非负整数 b，请计算 a × b。

回忆一下小学竖式乘法：用 b 去乘 a 的每一位，乘出来的结果可能不止一位（比如 `7 × 8 = 56`），于是就把个位留下、把十位以上的部分**进位**给下一位。高精度乘单精度用的正是这个办法。

（同样请用数组模拟，不要使用大整数库。）
