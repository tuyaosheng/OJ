C++ 里的 `int` 最大只能存到约 21 亿，`long long` 也只到约 9.2 × 10^18。可现实中经常要算更大的数——比如两个 500 位的整数相加，任何内置类型都装不下。

这时就要请出**高精度算法**：把大整数拆成一位一位，存进数组里，然后模仿我们小学列竖式的办法，逐位相加、逢十进一。

给定两个非负整数 a 和 b（可能非常大），请计算 a + b。

（本题是高精度的入门第一课，请**不要**使用 Python 大整数或 `__int128` 之类的取巧办法，老老实实用数组模拟竖式加法。）
