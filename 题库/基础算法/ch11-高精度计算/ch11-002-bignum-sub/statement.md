学会了高精度加法的"逢十进一"，接下来看它的反面——**逢十借一**。

给定两个非负整数 a 和 b（可能非常大），请计算 a − b。

注意：**结果可能是负数**（当 a < b 时），需要正确输出负号。

这道题有三个坑，正是高精度减法的全部难点：

1. 当 a < b 时怎么办？（提示：可以先比大小，用大的减小的，再决定要不要加负号）
2. 某一位不够减时怎么"借位"？
3. 计算结果可能出现前导零（如 `1000 − 999 = 0001`），怎么去掉？
