## 题目描述

这是【例3】疯狂的采药的直接变式——把"求最大价值"换成"求方案数"，正是上一章"过河卒"讲过的那个转化规律：转移方程从取最大值，变成把方案数加起来。

你有 `n` 种硬币，第 `i` 种面值是 `w[i]`，**每种硬币的数量不限**。请你求出恰好凑出金额 `S`，一共有多少种不同的**面值组合**（只关心每种面值各用了多少枚，不关心先后顺序——比如用一枚 1 元和一枚 2 元凑出 3 元，只算 1 种方案，不区分"先拿 1 元还是先拿 2 元"）。由于方案数可能很大，请输出方案数对 `1000000007` 取模的结果。
