## 题目描述

上一章"摆花"已经见过"用前几个东西凑出某个数量"这一类计数 DP。本章开始学习一类应用更广的 DP 模型——**背包问题**：有若干件物品，每件都有"代价"（比如重量）和"价值"，在总代价不超过某个上限的前提下，怎么选，能让总价值最大。

最经典的入门版本是**01 背包**：一个采药者有 `T` 分钟时间，山里有 `n 株`草药，采第 `i` 株草药要花 `t[i]` 分钟，采到手后能得到价值 `v[i]`（每株草药**最多采一次**，采不采由你决定，不能只采一部分）。请你求出在不超过 `T` 分钟的前提下，能采到的草药总价值最大是多少。
