## 题目描述

这是【例3】数列分段（最大值最小化）的直接变式，换一个应用场景，再练一遍"二分答案 + 贪心判定"这套组合拳。

图书馆有 `n` 本书排成一排（书的顺序不能打乱），第 `i` 本书有 `a[i]` 页。现在请 `m` 个抄写员来抄书，**每个抄写员负责连续的若干本书**（可以 0 本），每个人抄书的**总页数**互不干扰、同时进行。请你安排一种分配方案，使得**耗时最长的那个抄写员抄的页数尽可能少**，输出这个最少的最大页数。
