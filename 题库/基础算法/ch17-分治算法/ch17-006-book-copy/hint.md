- 和【例3】数列分段是同一个模型："把序列分成不超过 m 段，最小化最大段和"——这里"段"换成了"某个抄写员负责的连续几本书"，"允许 0 本"意味着实际用的抄写员数可以小于 m，等价于"不超过 m 段"。
- 二分范围、`check` 判定函数的写法都和例3完全一致：下界 `L = max(a[i])`，上界 `R = sum(a[i])`，`check(limit)` 贪心统计最少需要多少个抄写员才能保证人均页数不超过 `limit`。
- 做完这题，建议把这题和例3的代码放在一起对比读一遍——除了变量名和题面故事，核心逻辑应该几乎一模一样，这正说明你已经抓住了这一类模型的本质。
