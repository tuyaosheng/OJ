## 题目描述

本章开始学习几道更"综合"的动态规划经典题。前面几章的状态大多只涉及"一个位置"（一个数组下标、一个背包容量），这一章开始，状态经常需要**同时描述两个位置**——最典型的就是"两个字符串各自扫描到哪里"。

给定两个字符串 `A` 和 `B`，请你求出它们的**最长公共子序列**（LCS）的长度——子序列可以从原字符串里**跳着选**（不要求连续），但选出的字符必须保持在原字符串里的先后顺序。
