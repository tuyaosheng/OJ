竞技游戏的匹配系统要给玩家找"实力最接近"的对手。

对手池里有 n 名候选对手，第 i 名的天梯分是 b[i]。接下来有 m 名玩家排队匹配，第 j 名玩家的天梯分是 x[j]。

对每名玩家，系统会从对手池里挑出与其天梯分**差距最小**的候选，请输出这个**最小差距** |b[i] − x[j]|。

（对手池是共用的：每次匹配只是"查询"，不会把对手从池里拿走。）

n 和 m 都高达 10^5——对每名玩家把对手池扫一遍的 O(nm) 做法必然超时。**先把对手池排序，再对每名玩家二分查找**。
