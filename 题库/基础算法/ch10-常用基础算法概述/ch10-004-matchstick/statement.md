给你 n 根火柴棍，你可以拼出多少个形如 `A + B = C` 的等式？等式满足：
1. A、B、C 是**非负整数**；
2. A + B = C（正确的算式）；
3. 拼出所有数字、以及一个 `+` 和一个 `=`，**恰好**用掉 n 根火柴棍。

数字 0~9 用的火柴数分别是：6 2 5 5 4 5 6 3 7 6；`+` 用 2 根，`=` 用 2 根。

注意：`A + B = C` 与 `B + A = C`（A ≠ B 时）算作两个不同的等式。

（本题对应课件枚举思想，洛谷 P1149。）
