- 从头到尾扫描整个网格，遇到一个**还没被访问过的空地**，就说明发现了一个新的连通块（计数器 `+1`），然后从这个格子出发跑一次 BFS，把和它连通的所有格子都标记为"已访问"。
- 标记"已访问"这一步很关键：不能等格子出队才标记，**必须在入队的那一刻就标记**，否则同一个格子可能被重复放入队列，既浪费时间、也可能重复计数。
- BFS 版和 DFS 版的区别只在"用队列还是用递归/栈"来管理"待访问的格子"，标记访问、判断连通的逻辑完全一样——如果你在第 15 章写过 DFS 版本，把递归调用换成"入队"就是这题的解法。
