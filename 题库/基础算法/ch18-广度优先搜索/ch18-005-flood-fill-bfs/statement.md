## 题目描述

这是【例1】迷宫最短路径的直接变式——同样是在网格上做 BFS，但这次不求"最短路"，而是练习 BFS 的另一个经典用法：**洪水填充（Flood Fill）**，也就是"从一个格子出发，把和它连在一起的所有格子都标记为同一块"。第 15 章你已经用 DFS/回溯写过一次洪水填充，这次请改用 **BFS** 实现，体会两种写法的区别。

给定一个 `n` 行 `m` 列的网格，`.` 表示空地，`#` 表示障碍。如果两个空地格子**上下左右相邻**，就认为它们属于同一个连通块。请你求出整张网格一共有多少个连通块。
