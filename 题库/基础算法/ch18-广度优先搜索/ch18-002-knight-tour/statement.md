## 题目描述

【例1】的迷宫只有 4 个移动方向，这题换成中国象棋里的"马"——**每次移动是"日"字形**，一共有 8 种可能的走法。BFS 的框架完全不变，只是把"四个方向"换成"八种走法"。

给定一个 `n` 行 `m` 列的棋盘（没有障碍，格子都能落子），马从格子 `(x, y)` 出发，请你求出马到达棋盘上**每一个格子**最少需要走几步；如果某个格子马永远到不了，输出 `-1`。
