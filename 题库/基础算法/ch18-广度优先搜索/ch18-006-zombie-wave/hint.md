- 和【例4】烂橘子几乎是同一份代码：把所有刷新点（值为 `2`）**一次性**放进队列，`dist` 初始化为 `0`，然后正常做 BFS 扩散——区别只是【例4】只关心"最大的 dist 是多少"，这题要把**每个格子的 dist 都输出出来**。
- 障碍物（值为 `1`）不能进队，也不能被扩展进去，扩散时直接跳过。
- 没被 BFS 访问到的空地格子，`dist` 保持初始的 `-1`，直接输出即可，不需要额外判断。
- 别忘了输出量和【例2】马的遍历一样大（最多 100 万个数），注意用 `printf` 或加速过的 `cout`。
