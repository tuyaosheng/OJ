## 题目描述

前两题的 BFS 都是在"棋盘/网格"上扩散，这题要练一个更抽象的场景——**BFS 不一定要在格子上跑，只要能定义出"状态"和"状态之间怎么转移"，BFS 就能用**。这里的"格子"变成了**数轴上的一个点**。

农夫在数轴上的点 `n` 处，牛在数轴上的点 `k` 处（牛不会动）。农夫每分钟可以选择下面三种移动方式之一：

- 从 `x` 移动到 `x - 1`；
- 从 `x` 移动到 `x + 1`；
- 从 `x` 移动到 `2x`。

请你求出农夫抓到牛（到达 `k`）最少需要多少分钟。
