## 题目描述

前三题的 BFS 都**只有一个起点**。这题要练习**多源 BFS**——一次性有好几个起点，同时向外扩散。

有一个 `n` 行 `m` 列的仓库，每个格子是下面三种情况之一：

- `0` 表示空格子（没有橘子）；
- `1` 表示一个新鲜橘子；
- `2` 表示一个已经烂掉的橘子。

每分钟，每个烂橘子会使得它**上下左右四个方向**相邻的新鲜橘子也变烂。请你求出经过多少分钟后，仓库里**不存在新鲜橘子**；如果有新鲜橘子永远不会烂掉，输出 `-1`。如果仓库里一开始就没有新鲜橘子，输出 `0`。
