# 数据目录（数据库、测试用例、日志等不入库）
OnlineJudgeDeploy/data/
OnlineJudgeDeploy (旧系统，包括数据)/

# 备份文件
*.sql
*.dump
*.tar
*.tar.gz
backups/

# 部署用的镜像导出/传输中转文件（不入库，体积太大）
docker-images-export/
oj-backend-custom*.tar

# 远程操作过程中的截图（个人工作文件，不入库）
ubuntu截图/

# 生产服务器内网 IP / SSH 账号等私有部署笔记，只留本地，绝不入库
系统迁移.md

# 校本课选拔相关材料：含真实学生姓名、成绩、答题记录，绝不入库
校本课选拔题目/

# 教程课件 / 论文等大文件资料（不入库）
*.ppt
*.pptx
*.zip
3. 教程ppt课件*/
论文/

# 前端依赖和构建产物
OnlineJudgeFE/node_modules/
OnlineJudgeFE/dist/

# 题库：生成物不入库（都能一键重新生成，避免仓库膨胀）
#   tests/*.out      <- OnlineJudge/fps/build_chapter.py <章节目录>   （标程实跑生成）
#   题库/导入用XML/  <- OnlineJudge/fps/fps_generator.py <章节目录> -o <xml>
#   *_测试点数据.zip <- OnlineJudge/fps/pack_tests.py <章节目录>       （已被上面的 *.zip 覆盖）
# 保留的是【源素材】：config.json / 题面 md / solution.cpp / 讲解稿 / tests|samples 的 *.in / samples 的 *.out
题库/**/tests/*.out
题库/导入用XML/
题库/**/*.exe

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# 环境变量
.env
.env.local

# IDE
.idea/
.vscode/
*.suo
*.user

# 系统文件
.DS_Store
Thumbs.db

# Claude内部目录
.claude/
