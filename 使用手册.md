# 在线判题平台 — 使用手册

---

## 一、系统启动与关闭

### 日常启动

```powershell
cd E:\QingDaoOJ\OnlineJudgeDeploy
docker-compose up -d
```

启动后访问：
- **用户前台**：http://127.0.0.1
- **管理后台**：http://127.0.0.1/admin

> **注意**：必须用 `http://127.0.0.1`，不能用 `http://localhost`。
> Windows 上 localhost 会走 IPv6（被 WSL 占用），导致页面无法加载。

### 日常关闭

```powershell
cd E:\QingDaoOJ\OnlineJudgeDeploy
docker-compose down
```

> 数据存储在 Docker Volume 中，关闭后数据不会丢失。

---

## 二、修改前端代码后重新构建

修改 Vue 源码后需重新构建，然后重新打包 Docker 镜像：

```powershell
# 第一步：构建前端（必须先构建 DLL，再构建主包）
cd E:\QingDaoOJ\OnlineJudgeFE
$env:NODE_OPTIONS="--openssl-legacy-provider"
$env:NODE_ENV="production"
npm run build:dll
npm run build

# 第二步：重新构建 Docker 镜像并重启
cd E:\QingDaoOJ
docker build -f OnlineJudge/Dockerfile.custom -t oj-backend-custom:latest .
cd E:\QingDaoOJ\OnlineJudgeDeploy
docker-compose up -d --no-deps oj-backend
```

访问 **http://127.0.0.1** 即可看到更新后的前端。

---

## 三、前端开发调试（热重载，可选）

仅在需要实时预览改动时使用，不影响生产：

```powershell
cd E:\QingDaoOJ\OnlineJudgeFE
$env:NODE_ENV="development"
$env:NODE_OPTIONS="--openssl-legacy-provider"
$env:TARGET="http://127.0.0.1"
npm run dev
```

访问 **http://localhost:8080**，修改代码后页面自动刷新。

---

## 四、修改后端代码后重新构建

```powershell
# 必须从 monorepo 根目录执行
cd E:\QingDaoOJ
docker build -f OnlineJudge/Dockerfile.custom -t oj-backend-custom:latest .

cd E:\QingDaoOJ\OnlineJudgeDeploy
docker-compose up -d --no-deps oj-backend

# 如有新的数据库迁移：
docker exec onlinejudgedeploy-oj-backend-1 python manage.py migrate
```

---

## 五、管理员操作指南

访问 http://127.0.0.1/admin，使用超级管理员账号登录（默认 `root` / `rootroot`，**首次使用请立即修改**）。

### 5.1 网站 Logo

入口：系统设置 → 配置 → 网站 Logo

- 支持上传 jpg/png/gif/webp/svg，最大 5MB
- 上传后导航栏自动显示 Logo，浏览器标签图标同步更新

### 5.2 题目管理

| 操作 | 入口 |
|------|------|
| 新建 / 编辑题目 | 题目 → 题目列表 → 新建题目 |
| 上传题解视频 | 编辑题目页面顶部"题解视频"区域（需先保存题目） |
| FPS 批量导入 | 题目 → 导入导出 → 导入 FPS |

**FPS 导入扩展格式说明：**
```xml
<item>
  <id>T001</id>               <!-- 自定义显示 ID（可选） -->
  <tag>C++</tag>              <!-- 自定义标签，可多个（可选） -->
  <title>...</title>
  <test_input score="25">...  <!-- 指定单个测试点分数（可选） -->
  </test_input>
  ...
</item>
```

### 5.3 章节管理

入口：题目 → 章节管理

1. 新建章节，填写名称和描述
2. 点击"管理题目"，从全题库多选题目批量加入
3. 支持按 ID / 难度 / 通过率排序后筛选

### 5.4 课堂管理（上课模式）

入口：题目 → 课堂管理

1. **创建课堂**：绑定章节，设置开始 / 结束时间
2. **大屏看板**：点击课堂行的"大屏看板"按钮，新窗口打开投影界面
   - 实时显示全班学生 × 题目进度矩阵
   - ✓ 绿色 = 已通过 | — 黄色 = 尝试未通过 | 👁 蓝色 = 正在查看 | 灰色 = 未开始
   - 每 30 秒自动刷新
3. **IP 座位绑定**：在课堂管理页面下方手动录入 IP → 座位号对应关系

### 5.5 提交记录管理

入口：状态页（status?problemID=...）

- 管理员可在提交列表点击**"详情"**按钮查看每条提交的测试点详情
- 详情页可展开每个测试点，查看**输入 / 预期输出 / 实际输出**

### 5.6 用户管理

入口：用户

| 操作 | 说明 |
|------|------|
| 新建教师账号 | 点击"新建教师账号"按钮，教师不经注册页面创建 |
| 批量升年级 | 点击"批量升年级"按钮，所有学生年级 +1；可设置最大年级，超过则自动禁用（视为毕业） |
| 筛选用户 | 支持按身份（教师/学生）、年级筛选 |

### 5.7 AI 代码诊断

**配置（仅超级管理员）** 入口：常规 → AI 诊断配置

1. **API Base**：填 OpenAI 兼容接口地址，如 `https://api.deepseek.com/v1`（结尾不含 `/chat/completions`）
   - 兼容 DeepSeek / 通义千问 / Kimi / 智谱，以及本地 Ollama、vLLM
2. **模型名称**：如 `deepseek-chat`、`qwen-plus`、`gpt-4o-mini`
3. **API Key**：填入密钥（保存后不回显，留空表示不修改）
4. **每人每日次数上限**：默认 5（教师/管理员不受限）
5. **开放诊断的判题状态**：勾选哪些状态允许学生使用 AI 诊断
   - 默认**不勾选"编译错误"**（语法错误用 AI 诊断意义不大且浪费 token）
   - 未勾选的状态，学生端不显示按钮
6. 打开 **启用 AI 诊断** 开关 → 保存

> 关闭开关后，学生端不显示"AI 诊断"按钮。

**查看诊断记录（教师可见）** 入口：题目 → AI 诊断记录

- 列出所有学生的诊断记录，可按用户名 / 题目显示 ID 筛选
- 点击"查看诊断"查看大模型给出的完整分析

> 教师 / 管理员在前台打开学生的提交详情页时，也能看到已生成的诊断（按钮显示"查看已生成的 AI 诊断"）。

---

## 六、学生使用指南

1. 访问 http://127.0.0.1，点击右上角**注册**
2. 填写用户名、年级、班级、密码完成注册
3. 题目列表支持**列表模式**和**章节模式**切换
4. 课堂进行中时，页面顶部出现红色**"进入课堂"**按钮，点击进入专注视图
5. 提交代码后若**未通过**（管理员已开启 AI 诊断、且该状态被开放时），可用 AI 诊断分析错误原因与修改方向：
   - **做题页**：刚提交完出非 AC 结果时，状态区即时出现"AI 诊断"按钮
   - **提交详情页**：点开"我的提交"中任意一条失败记录均可诊断（推荐）；已诊断过的显示"查看已生成的 AI 诊断"，再看不消耗次数
   - 每天有次数限制，按钮旁显示"今日剩余 N 次"；同一条提交只算一次

---

## 七、版本管理（推送代码到 GitHub）

```powershell
cd E:\QingDaoOJ
git add .
git commit -m "改动说明"
git push
```

GitHub 仓库：https://github.com/tuyaosheng/OJ
